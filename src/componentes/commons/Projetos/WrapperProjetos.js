import styled from 'styled-components'
import { propToStyle } from '../../../theme/utils/propToStyle';


export const BaseWrapperProjetos = styled.div`
 background-color: #F7F3FF;
 ${propToStyle('marginRight')}
 ${propToStyle('marginLeft')}

`;

